/**
 * Languages the account's `language` setting can be set to from the Profile
 * page. With a single entry the Profile page shows the language read-only;
 * add entries here to offer a choice.
 */
export const LANGUAGE_OPTIONS: Array<{ value: string; label: string }> = [
  { value: "en", label: "English" },
];
